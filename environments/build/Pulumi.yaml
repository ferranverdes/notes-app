name: notes-infra-build
description: Provision and manage the foundational cloud infrastructure for the Notes platform using Pulumi.
main: ./index.js
runtime:
  name: nodejs
  options:
    typescript: false
    packagemanager: npm
config:
  pulumi:tags:
    value:
      pulumi:template: gcp-javascript
