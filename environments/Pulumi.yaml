name: notes-infra
description: Build and publish the Notes API Docker image to Google Artifact Registry using Pulumi.
main: ./index.js
runtime:
  name: nodejs
  options:
    typescript: false
    packagemanager: npm
config:
  pulumi:tags:
    value:
      pulumi:template: gcp-javascript
